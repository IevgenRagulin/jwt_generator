To run the JWT generator script:
0. Linux: You will need the xclip or xsel program. On debian/ubuntu this is: sudo apt-get install xclip. For details see https://github.com/janlelis/clipboard library that has been used for clipboard support
1. Run "bundle install"
2. Run "ruby launcher.rb"

To run the unit tests: bundle exec rspec